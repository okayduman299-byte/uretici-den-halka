import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ureticidenhalka.app',
  appName: 'Üreticiden Halka',
  webDir: 'www',
  server: {
    url: 'https://okayduman299-byte.github.io/uretici-den-halka/',
    cleartext: false
  }
};

export default config;
